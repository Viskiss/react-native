import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 10,
    borderWidth: 1,
    borderColor: '#333030',
    backgroundColor: '#d0cece',
  },
  text: {
    fontSize: 20,
    color: '#000',
    padding: 5,
    fontWeight: 'bold',
  },
});
