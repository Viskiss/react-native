import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 15,
  },
  input: {
    borderRadius: 20,
    backgroundColor: '#d0cece',
  },
  inputActive: {
    borderRadius: 20,
    borderWidth: 2,
    borderColor: '#333030',
  },
  labelError: {
    color: 'red',
    fontWeight: 'bold',
  },
});
